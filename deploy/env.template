# SMTP relay used by contact.php (via msmtp inside the container).
# Fill in and keep this file out of git — it lives only on the VPS (chmod 600).
SMTP_HOST=in-v3.mailjet.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=contact@big-emotion.com
