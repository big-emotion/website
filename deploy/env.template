# Next.js runtime — copy to .env on the VPS (chmod 600, never commit).
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Contact form SMTP relay — used by the /api/contact route (SWBE-31).
# Until that route is live, these values are unused.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=contact@big-emotion.com
