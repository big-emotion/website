# Build output and dependencies.
.next/
out/
build/
node_modules/

# Lockfile: owned by pnpm, rewritten on every install.
pnpm-lock.yaml

# Generated by `pnpm prismic:codegen` from the models — never hand-edited.
prismicio-types.d.ts

# Vendored third-party decoder binaries.
public/draco/

# A git worktree parked here is a second checkout of this same repo. Formatting
# it would rewrite another branch's tree — same rationale as the .claude ignore
# in eslint.config.mjs.
.claude/

# Content models are round-tripped by `pnpm prismic:pull`, which writes them
# with its own serializer. `pnpm prismic:check` diffs them structurally, so
# formatting is irrelevant to correctness — but reformatting them here would
# produce a spurious diff after every pull.
customtypes/
src/slices/**/model.json

# Rendered by `ferry-init`; regenerated wholesale on every Ferry version bump.
# Reformatting them here would be undone by the next `ferry-update`.
ferry-jira-*.beta.json
.github/workflows/ferry-router.yml
